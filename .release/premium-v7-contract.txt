HoldWise Premium V7 Contract
source_sha=938c83d276e1f7126573db439194cc0e8340893d
exit_code=1

TAP version 13
# Subtest: premium v7 exposes the five-part mobile navigation
not ok 1 - premium v7 exposes the five-part mobile navigation
  ---
  duration_ms: 4.793307
  type: 'test'
  location: '/home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:14:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /\/home/. Input:
    
    "import React from 'react';\n" +
      "import { QueryClient, QueryClientProvider } from '@tanstack/react-query';\n" +
      "import { HashRouter, Navigate, Route, Routes } from 'react-router-dom';\n" +
      "import { AppProvider } from '@/lib/appContext';\n" +
      "import CinematicBackdrop from '@/components/premium/CinematicBackdrop';\n" +
      "import CardAcademyLobby from '@/pages/CardAcademyLobby';\n" +
      "import GameRoom from '@/pages/GameRoom';\n" +
      "import GameTutorial from '@/pages/GameTutorial';\n" +
      "import DailyChallengeHub from '@/pages/DailyChallengeHub';\n" +
      '\n' +
      'const previewQueryClient = new QueryClient({\n' +
      '  defaultOptions: {\n' +
      '    queries: { refetchOnWindowFocus: false, retry: 1 },\n' +
      '  },\n' +
      '});\n' +
      '\n' +
      'export default function App() {\n' +
      '  return (\n' +
      '    <AppProvider>\n' +
      '      <QueryClientProvider client={previewQueryClient}>\n' +
      '        <HashRouter>\n' +
      '          <CinematicBackdrop intensity="normal" className="text-scale-root">\n' +
      '            <Routes>\n' +
      '              <Route path="/" element={<Navigate to="/academy" replace />} />\n' +
      '              <Route path="/academy" element={<CardAcademyLobby />} />\n' +
      '              <Route path="/game/:gameId" element={<GameRoom />} />\n' +
      '              <Route path="/game/:gameId/tutorial" element={<GameTutorial />} />\n' +
      '              <Route path="/daily-challenge" element={<DailyChallengeHub />} />\n' +
      '              <Route path="*" element={<Navigate to="/academy" replace />} />\n' +
      '            </Routes>\n' +
      '          </CinematicBackdrop>\n' +
      '        </HashRouter>\n' +
      '      </QueryClientProvider>\n' +
      '    </AppProvider>\n' +
      '  );\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import React from 'react';
    import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
    import { HashRouter, Navigate, Route, Routes } from 'react-router-dom';
    import { AppProvider } from '@/lib/appContext';
    import CinematicBackdrop from '@/components/premium/CinematicBackdrop';
    import CardAcademyLobby from '@/pages/CardAcademyLobby';
    import GameRoom from '@/pages/GameRoom';
    import GameTutorial from '@/pages/GameTutorial';
    import DailyChallengeHub from '@/pages/DailyChallengeHub';
    
    const previewQueryClient = new QueryClient({
      defaultOptions: {
        queries: { refetchOnWindowFocus: false, retry: 1 },
      },
    });
    
    export default function App() {
      return (
        <AppProvider>
          <QueryClientProvider client={previewQueryClient}>
            <HashRouter>
              <CinematicBackdrop intensity="normal" className="text-scale-root">
                <Routes>
                  <Route path="/" element={<Navigate to="/academy" replace />} />
                  <Route path="/academy" element={<CardAcademyLobby />} />
                  <Route path="/game/:gameId" element={<GameRoom />} />
                  <Route path="/game/:gameId/tutorial" element={<GameTutorial />} />
                  <Route path="/daily-challenge" element={<DailyChallengeHub />} />
                  <Route path="*" element={<Navigate to="/academy" replace />} />
                </Routes>
              </CinematicBackdrop>
            </HashRouter>
          </QueryClientProvider>
        </AppProvider>
      );
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:16:12)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.start (node:internal/test_runner/test:944:17)
    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)
  ...
# Subtest: premium v7 uses real licensed casino/card photography with a rotating fallback-safe atmosphere
not ok 2 - premium v7 uses real licensed casino/card photography with a rotating fallback-safe atmosphere
  ---
  duration_ms: 1.393823
  type: 'test'
  location: '/home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:22:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /images\.unsplash\.com\/photo-/. Input:
    
    'import React from "react";\n' +
      'import { cn } from "@/lib/utils";\n' +
      'import { useApp } from "@/lib/appContext";\n' +
      '\n' +
      'const FLOATING_CARDS = [\n' +
      '  { id: "ace-spades", rank: "A", suit: "♠", tone: "ink" },\n' +
      '  { id: "queen-hearts", rank: "Q", suit: "♥", tone: "coral" },\n' +
      '  { id: "king-clubs", rank: "K", suit: "♣", tone: "ink" },\n' +
      '  { id: "ten-diamonds", rank: "10", suit: "♦", tone: "coral" },\n' +
      '  { id: "seven-spades", rank: "7", suit: "♠", tone: "royal" },\n' +
      '  { id: "joker-star", rank: "★", suit: "✦", tone: "royal" },\n' +
      '];\n' +
      '\n' +
      'export default function CinematicBackdrop({ children, intensity = "normal", className = "" }) {\n' +
      '  const { accessibility } = useApp();\n' +
      '  return (\n' +
      '    <div className={cn("relative min-h-screen overflow-x-hidden hw-felt-bg", className)} data-intensity={intensity}>\n' +
      '      <div\n' +
      '        aria-hidden="true"\n' +
      '        className={cn("pointer-events-none fixed inset-0 hw-cinematic-breathe", accessibility.reducedMotion && "!animate-none")}\n' +
      '      />\n' +
      '      <div aria-hidden="true" className="pointer-events-none fixed inset-0 hw-cinematic-suits" />\n' +
      '      <div aria-hidden="true" className="pointer-events-none fixed inset-0 hw-card-room-scene">\n' +
      '        {FLOATING_CARDS.map((card, index) => (\n' +
      '          <span\n' +
      '            key={card.id}\n' +
      '            data-card={card.id}\n' +
      '            className={cn(\n' +
      '              "hw-floating-card",\n' +
      '              `hw-floating-card-${index + 1}`,\n' +
      '              `hw-floating-card-${card.tone}`,\n' +
      '              accessibility.reducedMotion && "!animate-none"\n' +
      '            )}\n' +
      '          >\n' +
      '            <span className="hw-floating-rank">{card.rank}</span>\n' +
      '            <span className="hw-floating-suit">{card.suit}</span>\n' +
      '          </span>\n' +
      '        ))}\n' +
      '      </div>\n' +
      '      <div aria-hidden="true" className="pointer-events-none fixed inset-0 hw-cinematic-vignette" />\n' +
      '      <div className="relative z-10 min-h-screen">{children}</div>\n' +
      '    </div>\n' +
      '  );\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import React from "react";
    import { cn } from "@/lib/utils";
    import { useApp } from "@/lib/appContext";
    
    const FLOATING_CARDS = [
      { id: "ace-spades", rank: "A", suit: "♠", tone: "ink" },
      { id: "queen-hearts", rank: "Q", suit: "♥", tone: "coral" },
      { id: "king-clubs", rank: "K", suit: "♣", tone: "ink" },
      { id: "ten-diamonds", rank: "10", suit: "♦", tone: "coral" },
      { id: "seven-spades", rank: "7", suit: "♠", tone: "royal" },
      { id: "joker-star", rank: "★", suit: "✦", tone: "royal" },
    ];
    
    export default function CinematicBackdrop({ children, intensity = "normal", className = "" }) {
      const { accessibility } = useApp();
      return (
        <div className={cn("relative min-h-screen overflow-x-hidden hw-felt-bg", className)} data-intensity={intensity}>
          <div
            aria-hidden="true"
            className={cn("pointer-events-none fixed inset-0 hw-cinematic-breathe", accessibility.reducedMotion && "!animate-none")}
          />
          <div aria-hidden="true" className="pointer-events-none fixed inset-0 hw-cinematic-suits" />
          <div aria-hidden="true" className="pointer-events-none fixed inset-0 hw-card-room-scene">
            {FLOATING_CARDS.map((card, index) => (
              <span
                key={card.id}
                data-card={card.id}
                className={cn(
                  "hw-floating-card",
                  `hw-floating-card-${index + 1}`,
                  `hw-floating-card-${card.tone}`,
                  accessibility.reducedMotion && "!animate-none"
                )}
              >
                <span className="hw-floating-rank">{card.rank}</span>
                <span className="hw-floating-suit">{card.suit}</span>
              </span>
            ))}
          </div>
          <div aria-hidden="true" className="pointer-events-none fixed inset-0 hw-cinematic-vignette" />
          <div className="relative z-10 min-h-screen">{children}</div>
        </div>
      );
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:23:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
  ...
# Subtest: casino ambience is opt-in and persisted with off low high levels
not ok 3 - casino ambience is opt-in and persisted with off low high levels
  ---
  duration_ms: 8.434153
  type: 'test'
  location: '/home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:31:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /casinoAmbience:\s*["']off["']/. Input:
    
    'import React, { createContext, useContext, useEffect, useMemo, useRef, useState } from "react";\n' +
      'import { base44 } from "@/api/base44Client";\n' +
      '\n' +
      '// Global app context: profile, accessibility, theme, premium, coach usage.\n' +
      '// Guest progress is stored in localStorage; registered users sync via entities.\n' +
      '\n' +
      'const STORAGE_KEY = "holdwise_state_v1";\n' +
      'const CLOUD_LOCAL_KEYS = {\n' +
      '  lessonProgress: "holdwise_lesson_progress_v1",\n' +
      '  practiceStats: "holdwise_vp_stats_v1",\n' +
      '  mistakes: "holdwise_mistakes_v1",\n' +
      '  handCount: "holdwise_vp_handcount_v1",\n' +
      '};\n' +
      '\n' +
      'function readLocalJson(key, fallback) {\n' +
      '  try { return JSON.parse(localStorage.getItem(key) || "null") ?? fallback; } catch { return fallback; }\n' +
      '}\n' +
      '\n' +
      'function writeLocalJson(key, value) {\n' +
      '  if (value !== undefined && value !== null) localStorage.setItem(key, JSON.stringify(value));\n' +
      '}\n' +
      '\n' +
      '\n' +
      'const defaultAccessibility = {\n' +
      '  largeCardMode: false,\n' +
      '  highContrast: false,\n' +
      '  colorBlindSuitIndicators: true,\n' +
      '  reducedMotion: false,\n' +
      '  haptics: true,\n' +
      '  plainLanguage: false,\n' +
      '  leftHanded: false,\n' +
      '  dynamicTextSize: "medium",\n' +
      '  rankNarration: false,\n' +
      '};\n' +
      '\n' +
      'const defaultSettings = {\n' +
      '  soundEffects: true,\n' +
      '  voiceEnabled: "ask-later", // enabled | text-only | ask-later\n' +
      '  theme: "light",\n' +
      '  mascot: "ace", // mascot id | "none"\n' +
      '  notifications: {\n' +
      '    dailyLesson: true,\n' +
      '    streakReminder: true,\n' +
      '    newCourse: false,\n' +
      '    weeklySummary: true,\n' +
      '  },\n' +
      '};\n' +
      '\n' +
      'const defaultProfile = {\n' +
      '  displayName: "Guest",\n' +
      '  avatar: "ace",\n' +
      '  ageConfirmed: false,\n' +
      '  skillLevel: "new-to-cards", // new-to-cards | knows-cards | knows-poker | improve-vp | other-games\n' +
      '  learningStyle: "mixture", // simple | visual | practice | math | mixture\n' +
      '  onboardingComplete: false,\n' +
      '  level: "New Learner",\n' +
      '  learningPoints: 0,\n' +
      '  streak: 0,\n' +
      '  lastActiveDate: null,\n' +
      '};\n' +
      '\n' +
      'function loadState() {\n' +
      '  try {\n' +
      '    const raw = localStorage.getItem(STORAGE_KEY);\n' +
      '    if (raw) return JSON.parse(raw);\n' +
      '  } catch (e) {}\n' +
      '  return null;\n' +
      '}\n' +
      '\n' +
      'const AppContext = createContext(null);\n' +
      '\n' +
      'export function AppProvider({ children }) {\n' +
      '  const persisted = loadState();\n' +
      '  const [profile, setProfile] = useState(persisted?.profile || defaultProfile);\n' +
      '  const [accessibility, setAccessibility] = useState(persisted?.accessibility || defaultAccessibility);\n' +
      '  const [settings, setSettings] = useState(persisted?.settings || defaultSettings);\n' +
      '  const [premium, setPremium] = useState({ status: "free", platform: null, productId: null, verified: false });\n' +
      '  const [coachUsage, setCoachUsage] = useState(() => {\n' +
      '    const saved = persisted?.coachUsage;\n' +
      '    return saved?.date === today() ? saved : { date: today(), count: 0 };\n' +
      '  });\n' +
      '  const [mascotState, setMascotState] = useState({ mood: "idle", message: null, at: 0 });\n' +
      '  const [user, setUser] = useState(null);\n' +
      '  const [authReady, setAuthReady] = useState(false);\n' +
      '  const cloudRecordId = useRef(null);\n' +
      '  const cloudHydrated = useRef(false);\n' +
      '\n' +
      '  // Show a mascot reaction, then ease back to idle. Optional message overrides the mascot line.\n' +
      '  const setMascotMood = (mood, message = null, holdMs = 2600) => {\n' +
      '    setMascotState({ mood, message, at: Date.now() });\n' +
      '    if (holdMs > 0) {\n' +
      '      setTimeout(() => setMascotState((s) => (s.mood === mood ? { mood: "idle", message: null, at: Date.now() } : s)), holdMs);\n' +
      '    }\n' +
      '  };\n' +
      '\n' +
      '  // Apply theme + accessibility to <html>.\n' +
      '  useEffect(() => {\n' +
      '    const root = document.documentElement;\n' +
      '    root.classList.toggle("dark", settings.theme === "dark");\n' +
      '    root.classList.toggle("light", settings.theme === "light");\n' +
      '    root.style.setProperty("--hw-large-card", accessibility.largeCardMode ? "1" : "0");\n' +
      '    root.style.setProperty("--hw-text-scale", textScale(accessibility.dynamicTextSize));\n' +
      '    if (accessibility.reducedMotion) root.classList.add("reduce-motion");\n' +
      '    else root.classList.remove("reduce-motion");\n' +
      '    if (accessibility.highContrast) root.classList.add("high-contrast");\n' +
      '    else root.classList.remove("high-contrast");\n' +
      '    if (accessibility.leftHanded) root.classList.add("left-handed");\n' +
      '    else root.classList.remove("left-handed");\n' +
      '  }, [settings.theme, accessibility]);\n' +
      '\n' +
      '  // Persist locally (guest progress).\n' +
      '  useEffect(() => {\n' +
      '    localStorage.setItem(STORAGE_KEY, JSON.stringify({ profile, accessibility, settings, coachUsage }));\n' +
      '  }, [profile, accessibility, settings, coachUsage]);\n' +
      '\n' +
      '  // Reset daily coach usage.\n' +
      '  useEffect(() => {\n' +
      '    if (coachUsage.date !== today()) setCoachUsage({ date: today(), count: 0 });\n' +
      '  }, [coachUsage]);\n' +
      '\n' +
      '  // Auth init.\n' +
      '  useEffect(() => {\n' +
      '    let active = true;\n' +
      '    base44.auth\n' +
      '      .isAuthenticated()\n' +
      '      .then((ok) => {\n' +
      '        if (!active) return;\n' +
      '        if (ok) {\n' +
      '          base44.auth\n' +
      '            .me()\n' +
      '            .then((u) => { if (active) setUser(u); })\n' +
      '            .catch(() => {})\n' +
      '            .finally(() => active && setAuthReady(true));\n' +
      '        } else {\n' +
      '          setAuthReady(true);\n' +
      '        }\n' +
      '      })\n' +
      '      .catch(() => setAuthReady(true));\n' +
      '    return () => { active = false; };\n' +
      '  }, []);\n' +
      '\n' +
      '  // Hydrate registered learners from one cloud state record, then keep the\n' +
      '  // local-first experience synchronized in the background.\n' +
      '  useEffect(() => {\n' +
      '    if (!user?.id) { cloudHydrated.current = false; cloudRecordId.current = null; return undefined; }\n' +
      '    let active = true;\n' +
      '    (async () => {\n' +
      '      try {\n' +
      '        const records = await base44.entities.LearningState.filter({ userId: user.id });\n' +
      '        if (!active) return;\n' +
      '        const record = records?.[0];\n' +
      '        if (record) {\n' +
      '          cloudRecordId.current = record.id;\n' +
      '          if (record.profile) setProfile((current) => ({ ...current, ...record.profile }));\n' +
      '          if (record.accessibility) setAccessibility((current) => ({ ...current, ...record.accessibility }));\n' +
      '          if (record.settings) setSettings((current) => ({ ...current, ...record.settings }));\n' +
      '          if (record.coachUsage?.date) setCoachUsage(record.coachUsage);\n' +
      '          for (const [field, key] of Object.entries(CLOUD_LOCAL_KEYS)) writeLocalJson(key, record[field]);\n' +
      '        }\n' +
      '      } catch {\n' +
      '        // Local mode remains fully usable when cloud sync is temporarily unavailable.\n' +
      '      } finally {\n' +
      '        if (active) cloudHydrated.current = true;\n' +
      '      }\n' +
      '    })();\n' +
      '    return () => { active = false; };\n' +
      '  }, [user?.id]);\n' +
      '\n' +
      '  useEffect(() => {\n' +
      '    if (!user?.id) return undefined;\n' +
      '    let stopped = false;\n' +
      '    async function syncNow() {\n' +
      '      if (stopped || !cloudHydrated.current) return;\n' +
      '      const payload = {\n' +
      '        userId: user.id,\n' +
      '        profile,\n' +
      '        accessibility,\n' +
      '        settings,\n' +
      '        coachUsage,\n' +
      '        lessonProgress: readLocalJson(CLOUD_LOCAL_KEYS.lessonProgress, {}),\n' +
      '        practiceStats: readLocalJson(CLOUD_LOCAL_KEYS.practiceStats, {}),\n' +
      '        mistakes: readLocalJson(CLOUD_LOCAL_KEYS.mistakes, []),\n' +
      '        handCount: readLocalJson(CLOUD_LOCAL_KEYS.handCount, {}),\n' +
      '        updatedAt: new Date().toISOString(),\n' +
      '      };\n' +
      '      try {\n' +
      '        if (cloudRecordId.current) {\n' +
      '          await base44.entities.LearningState.update(cloudRecordId.current, payload);\n' +
      '        } else {\n' +
      '          const created = await base44.entities.LearningState.create(payload);\n' +
      '          cloudRecordId.current = created?.id || null;\n' +
      '        }\n' +
      '      } catch {\n' +
      '        // The next scheduled pass retries without interrupting the learner.\n' +
      '      }\n' +
      '    }\n' +
      '    const debounce = window.setTimeout(syncNow, 1800);\n' +
      '    const interval = window.setInterval(syncNow, 20000);\n' +
      '    const onVisibility = () => { if (document.visibilityState === "hidden") syncNow(); };\n' +
      '    document.addEventListener("visibilitychange", onVisibility);\n' +
      '    return () => { stopped = true; window.clearTimeout(debounce); window.clearInterval(interval); document.removeEventListener("visibilitychange", onVisibility); };\n' +
      '  }, [user?.id, profile, accessibility, settings, coachUsage]);\n' +
      '\n' +
      '  const value = useMemo(\n' +
      '    () => ({\n' +
      '      user,\n' +
      '      setUser,\n' +
      '      authReady,\n' +
      '      profile,\n' +
      '      setProfile,\n' +
      '      accessibility,\n' +
      '      setAccessibility,\n' +
      '      settings,\n' +
      '      setSettings,\n' +
      '      premium,\n' +
      '      setPremium,\n' +
      '      coachUsage,\n' +
      '      mascotState,\n' +
      '      setMascotMood,\n' +
      '      incrementCoach: () => setCoachUsage((c) => ({ date: today(), count: c.count + 1 })),\n' +
      '      resetCoach: () => setCoachUsage({ date: today(), count: 0 }),\n' +
      '      isPremium: premium.status === "active" || premium.status === "lifetime",\n' +
      '      canAskCoach: () => {\n' +
      '        if (premium.status === "active" || premium.status === "lifetime") return true;\n' +
      '        return coachUsage.count < 5;\n' +
      '      },\n' +
      '      remainingCoachQuestions:\n' +
      '        premium.status === "active" || premium.status === "lifetime"\n' +
      '          ? Infinity\n' +
      '          : Math.max(0, 5 - coachUsage.count),\n' +
      '      addPoints: (n) => setProfile((p) => ({ ...p, learningPoints: p.learningPoints + n })),\n' +
      '      bumpStreak: () =>\n' +
      '        setProfile((p) => {\n' +
      '          const t = today();\n' +
      '          if (p.lastActiveDate === t) return p;\n' +
      '          const yesterday = addDays(t, -1);\n' +
      '          const streak = p.lastActiveDate === yesterday ? p.streak + 1 : 1;\n' +
      '          return { ...p, streak, lastActiveDate: t };\n' +
      '        }),\n' +
      '    }),\n' +
      '    [user, authReady, profile, accessibility, settings, premium, coachUsage, mascotState]\n' +
      '  );\n' +
      '\n' +
      '  return <AppContext.Provider value={value}>{children}</AppContext.Provider>;\n' +
      '}\n' +
      '\n' +
      'export function useApp() {\n' +
      '  const ctx = useContext(AppContext);\n' +
      '  if (!ctx) throw new Error("useApp must be used within AppProvider");\n' +
      '  return ctx;\n' +
      '}\n' +
      '\n' +
      'function today() {\n' +
      '  return formatLocalDate(new Date());\n' +
      '}\n' +
      'function addDays(dateStr, n) {\n' +
      '  const [year, month, day] = dateStr.split("-").map(Number);\n' +
      '  const date = new Date(year, month - 1, day);\n' +
      '  date.setDate(date.getDate() + n);\n' +
      '  return formatLocalDate(date);\n' +
      '}\n' +
      'function formatLocalDate(date) {\n' +
      '  const year = date.getFullYear();\n' +
      '  const month = String(date.getMonth() + 1).padStart(2, "0");\n' +
      '  const day = String(date.getDate()).padStart(2, "0");\n' +
      '  return `${year}-${month}-${day}`;\n' +
      '}\n' +
      'function textScale(size) {\n' +
      '  return { small: "0.92", medium: "1", large: "1.12", xlarge: "1.25" }[size] || "1";\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import React, { createContext, useContext, useEffect, useMemo, useRef, useState } from "react";
    import { base44 } from "@/api/base44Client";
    
    // Global app context: profile, accessibility, theme, premium, coach usage.
    // Guest progress is stored in localStorage; registered users sync via entities.
    
    const STORAGE_KEY = "holdwise_state_v1";
    const CLOUD_LOCAL_KEYS = {
      lessonProgress: "holdwise_lesson_progress_v1",
      practiceStats: "holdwise_vp_stats_v1",
      mistakes: "holdwise_mistakes_v1",
      handCount: "holdwise_vp_handcount_v1",
    };
    
    function readLocalJson(key, fallback) {
      try { return JSON.parse(localStorage.getItem(key) || "null") ?? fallback; } catch { return fallback; }
    }
    
    function writeLocalJson(key, value) {
      if (value !== undefined && value !== null) localStorage.setItem(key, JSON.stringify(value));
    }
    
    
    const defaultAccessibility = {
      largeCardMode: false,
      highContrast: false,
      colorBlindSuitIndicators: true,
      reducedMotion: false,
      haptics: true,
      plainLanguage: false,
      leftHanded: false,
      dynamicTextSize: "medium",
      rankNarration: false,
    };
    
    const defaultSettings = {
      soundEffects: true,
      voiceEnabled: "ask-later", // enabled | text-only | ask-later
      theme: "light",
      mascot: "ace", // mascot id | "none"
      notifications: {
        dailyLesson: true,
        streakReminder: true,
        newCourse: false,
        weeklySummary: true,
      },
    };
    
    const defaultProfile = {
      displayName: "Guest",
      avatar: "ace",
      ageConfirmed: false,
      skillLevel: "new-to-cards", // new-to-cards | knows-cards | knows-poker | improve-vp | other-games
      learningStyle: "mixture", // simple | visual | practice | math | mixture
      onboardingComplete: false,
      level: "New Learner",
      learningPoints: 0,
      streak: 0,
      lastActiveDate: null,
    };
    
    function loadState() {
      try {
        const raw = localStorage.getItem(STORAGE_KEY);
        if (raw) return JSON.parse(raw);
      } catch (e) {}
      return null;
    }
    
    const AppContext = createContext(null);
    
    export function AppProvider({ children }) {
      const persisted = loadState();
      const [profile, setProfile] = useState(persisted?.profile || defaultProfile);
      const [accessibility, setAccessibility] = useState(persisted?.accessibility || defaultAccessibility);
      const [settings, setSettings] = useState(persisted?.settings || defaultSettings);
      const [premium, setPremium] = useState({ status: "free", platform: null, productId: null, verified: false });
      const [coachUsage, setCoachUsage] = useState(() => {
        const saved = persisted?.coachUsage;
        return saved?.date === today() ? saved : { date: today(), count: 0 };
      });
      const [mascotState, setMascotState] = useState({ mood: "idle", message: null, at: 0 });
      const [user, setUser] = useState(null);
      const [authReady, setAuthReady] = useState(false);
      const cloudRecordId = useRef(null);
      const cloudHydrated = useRef(false);
    
      // Show a mascot reaction, then ease back to idle. Optional message overrides the mascot line.
      const setMascotMood = (mood, message = null, holdMs = 2600) => {
        setMascotState({ mood, message, at: Date.now() });
        if (holdMs > 0) {
          setTimeout(() => setMascotState((s) => (s.mood === mood ? { mood: "idle", message: null, at: Date.now() } : s)), holdMs);
        }
      };
    
      // Apply theme + accessibility to <html>.
      useEffect(() => {
        const root = document.documentElement;
        root.classList.toggle("dark", settings.theme === "dark");
        root.classList.toggle("light", settings.theme === "light");
        root.style.setProperty("--hw-large-card", accessibility.largeCardMode ? "1" : "0");
        root.style.setProperty("--hw-text-scale", textScale(accessibility.dynamicTextSize));
        if (accessibility.reducedMotion) root.classList.add("reduce-motion");
        else root.classList.remove("reduce-motion");
        if (accessibility.highContrast) root.classList.add("high-contrast");
        else root.classList.remove("high-contrast");
        if (accessibility.leftHanded) root.classList.add("left-handed");
        else root.classList.remove("left-handed");
      }, [settings.theme, accessibility]);
    
      // Persist locally (guest progress).
      useEffect(() => {
        localStorage.setItem(STORAGE_KEY, JSON.stringify({ profile, accessibility, settings, coachUsage }));
      }, [profile, accessibility, settings, coachUsage]);
    
      // Reset daily coach usage.
      useEffect(() => {
        if (coachUsage.date !== today()) setCoachUsage({ date: today(), count: 0 });
      }, [coachUsage]);
    
      // Auth init.
      useEffect(() => {
        let active = true;
        base44.auth
          .isAuthenticated()
          .then((ok) => {
            if (!active) return;
            if (ok) {
              base44.auth
                .me()
                .then((u) => { if (active) setUser(u); })
                .catch(() => {})
                .finally(() => active && setAuthReady(true));
            } else {
              setAuthReady(true);
            }
          })
          .catch(() => setAuthReady(true));
        return () => { active = false; };
      }, []);
    
      // Hydrate registered learners from one cloud state record, then keep the
      // local-first experience synchronized in the background.
      useEffect(() => {
        if (!user?.id) { cloudHydrated.current = false; cloudRecordId.current = null; return undefined; }
        let active = true;
        (async () => {
          try {
            const records = await base44.entities.LearningState.filter({ userId: user.id });
            if (!active) return;
            const record = records?.[0];
            if (record) {
              cloudRecordId.current = record.id;
              if (record.profile) setProfile((current) => ({ ...current, ...record.profile }));
              if (record.accessibility) setAccessibility((current) => ({ ...current, ...record.accessibility }));
              if (record.settings) setSettings((current) => ({ ...current, ...record.settings }));
              if (record.coachUsage?.date) setCoachUsage(record.coachUsage);
              for (const [field, key] of Object.entries(CLOUD_LOCAL_KEYS)) writeLocalJson(key, record[field]);
            }
          } catch {
            // Local mode remains fully usable when cloud sync is temporarily unavailable.
          } finally {
            if (active) cloudHydrated.current = true;
          }
        })();
        return () => { active = false; };
      }, [user?.id]);
    
      useEffect(() => {
        if (!user?.id) return undefined;
        let stopped = false;
        async function syncNow() {
          if (stopped || !cloudHydrated.current) return;
          const payload = {
            userId: user.id,
            profile,
            accessibility,
            settings,
            coachUsage,
            lessonProgress: readLocalJson(CLOUD_LOCAL_KEYS.lessonProgress, {}),
            practiceStats: readLocalJson(CLOUD_LOCAL_KEYS.practiceStats, {}),
            mistakes: readLocalJson(CLOUD_LOCAL_KEYS.mistakes, []),
            handCount: readLocalJson(CLOUD_LOCAL_KEYS.handCount, {}),
            updatedAt: new Date().toISOString(),
          };
          try {
            if (cloudRecordId.current) {
              await base44.entities.LearningState.update(cloudRecordId.current, payload);
            } else {
              const created = await base44.entities.LearningState.create(payload);
              cloudRecordId.current = created?.id || null;
            }
          } catch {
            // The next scheduled pass retries without interrupting the learner.
          }
        }
        const debounce = window.setTimeout(syncNow, 1800);
        const interval = window.setInterval(syncNow, 20000);
        const onVisibility = () => { if (document.visibilityState === "hidden") syncNow(); };
        document.addEventListener("visibilitychange", onVisibility);
        return () => { stopped = true; window.clearTimeout(debounce); window.clearInterval(interval); document.removeEventListener("visibilitychange", onVisibility); };
      }, [user?.id, profile, accessibility, settings, coachUsage]);
    
      const value = useMemo(
        () => ({
          user,
          setUser,
          authReady,
          profile,
          setProfile,
          accessibility,
          setAccessibility,
          settings,
          setSettings,
          premium,
          setPremium,
          coachUsage,
          mascotState,
          setMascotMood,
          incrementCoach: () => setCoachUsage((c) => ({ date: today(), count: c.count + 1 })),
          resetCoach: () => setCoachUsage({ date: today(), count: 0 }),
          isPremium: premium.status === "active" || premium.status === "lifetime",
          canAskCoach: () => {
            if (premium.status === "active" || premium.status === "lifetime") return true;
            return coachUsage.count < 5;
          },
          remainingCoachQuestions:
            premium.status === "active" || premium.status === "lifetime"
              ? Infinity
              : Math.max(0, 5 - coachUsage.count),
          addPoints: (n) => setProfile((p) => ({ ...p, learningPoints: p.learningPoints + n })),
          bumpStreak: () =>
            setProfile((p) => {
              const t = today();
              if (p.lastActiveDate === t) return p;
              const yesterday = addDays(t, -1);
              const streak = p.lastActiveDate === yesterday ? p.streak + 1 : 1;
              return { ...p, streak, lastActiveDate: t };
            }),
        }),
        [user, authReady, profile, accessibility, settings, premium, coachUsage, mascotState]
      );
    
      return <AppContext.Provider value={value}>{children}</AppContext.Provider>;
    }
    
    export function useApp() {
      const ctx = useContext(AppContext);
      if (!ctx) throw new Error("useApp must be used within AppProvider");
      return ctx;
    }
    
    function today() {
      return formatLocalDate(new Date());
    }
    function addDays(dateStr, n) {
      const [year, month, day] = dateStr.split("-").map(Number);
      const date = new Date(year, month - 1, day);
      date.setDate(date.getDate() + n);
      return formatLocalDate(date);
    }
    function formatLocalDate(date) {
      const year = date.getFullYear();
      const month = String(date.getMonth() + 1).padStart(2, "0");
      const day = String(date.getDate()).padStart(2, "0");
      return `${year}-${month}-${day}`;
    }
    function textScale(size) {
      return { small: "0.92", medium: "1", large: "1.12", xlarge: "1.25" }[size] || "1";
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:32:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: the rebuild adds functional home games practice learn and progress hubs
not ok 4 - the rebuild adds functional home games practice learn and progress hubs
  ---
  duration_ms: 0.782297
  type: 'test'
  location: '/home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:41:1'
  failureType: 'testCodeFailure'
  error: 'Missing src/pages/PremiumHome.jsx'
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected: true
  actual: false
  operator: '=='
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:48:13)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: all real game tables inherit the premium stage without replacing the engines
not ok 5 - all real game tables inherit the premium stage without replacing the engines
  ---
  duration_ms: 0.583855
  type: 'test'
  location: '/home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:51:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /hw-premium-game-stage/. Input:
    
    "import React from 'react';\n" +
      "import { ArrowLeft, BookOpen, Home, Sparkles } from 'lucide-react';\n" +
      "import { useNavigate } from 'react-router-dom';\n" +
      "import GlassSurface from '@/components/premium/GlassSurface';\n" +
      "import TactilePressable from '@/components/premium/TactilePressable';\n" +
      "import AskCoachButton from '@/components/AskCoachButton';\n" +
      "import SensoryControls from '@/components/premium/SensoryControls';\n" +
      '\n' +
      'export default function GameShell({ game, children, coachContext = null, actions = null }) {\n' +
      '  const navigate = useNavigate();\n' +
      '  return (\n' +
      "    <div className={`hw-game-shell min-h-screen px-3 pb-8 pt-[max(1rem,env(safe-area-inset-top))] text-[hsl(var(--hw-ink))] hw-game-${game?.family || 'cards'}`}>\n" +
      '      <GlassSurface strength={3} className="mx-auto mb-3 flex max-w-5xl items-center justify-between gap-2 rounded-2xl p-2.5">\n' +
      '        <div className="flex items-center gap-2">\n' +
      `          <TactilePressable onClick={() => navigate('/academy')} className="hw-shell-icon rounded-xl p-2 shadow-none" aria-label="Back to Card Academy"><ArrowLeft size={19} /></TactilePressable>\n` +
      '          <div>\n' +
      '            <p className="text-[9px] font-bold uppercase tracking-[0.18em] hw-royal-text">HoldWise Card Academy</p>\n' +
      `            <h1 className="font-heading text-lg font-black leading-tight text-[hsl(var(--hw-ink))]">{game?.title || 'Card Game'}</h1>\n` +
      '          </div>\n' +
      '        </div>\n' +
      '        <div className="flex items-center gap-1.5">\n' +
      '          <SensoryControls className="hidden sm:flex" />\n' +
      '          {actions}\n' +
      '          <TactilePressable onClick={() => navigate(`/game/${game?.id}/tutorial`)} className="hw-shell-icon rounded-xl p-2 shadow-none" aria-label={`Tutorial for ${game?.title || \'game\'}`}><BookOpen size={18} /></TactilePressable>\n' +
      `          <TactilePressable onClick={() => navigate('/academy')} className="hw-shell-icon rounded-xl p-2 shadow-none" aria-label="Academy home"><Home size={18} /></TactilePressable>\n` +
      '        </div>\n' +
      '      </GlassSurface>\n' +
      '      <main className="mx-auto max-w-5xl">{children}</main>\n' +
      '      <div className="mx-auto mt-4 flex max-w-5xl items-center justify-between gap-3">\n' +
      '        <span className="flex items-center gap-1.5 text-[11px] text-[hsl(var(--hw-ink)/.58)]"><Sparkles size={13} className="hw-sunshine-text" /> Full play · Coach-enabled</span>\n' +
      '        <AskCoachButton context={coachContext} label="Ask Coach Ace" />\n' +
      '      </div>\n' +
      '    </div>\n' +
      '  );\n' +
      '}\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    import React from 'react';
    import { ArrowLeft, BookOpen, Home, Sparkles } from 'lucide-react';
    import { useNavigate } from 'react-router-dom';
    import GlassSurface from '@/components/premium/GlassSurface';
    import TactilePressable from '@/components/premium/TactilePressable';
    import AskCoachButton from '@/components/AskCoachButton';
    import SensoryControls from '@/components/premium/SensoryControls';
    
    export default function GameShell({ game, children, coachContext = null, actions = null }) {
      const navigate = useNavigate();
      return (
        <div className={`hw-game-shell min-h-screen px-3 pb-8 pt-[max(1rem,env(safe-area-inset-top))] text-[hsl(var(--hw-ink))] hw-game-${game?.family || 'cards'}`}>
          <GlassSurface strength={3} className="mx-auto mb-3 flex max-w-5xl items-center justify-between gap-2 rounded-2xl p-2.5">
            <div className="flex items-center gap-2">
              <TactilePressable onClick={() => navigate('/academy')} className="hw-shell-icon rounded-xl p-2 shadow-none" aria-label="Back to Card Academy"><ArrowLeft size={19} /></TactilePressable>
              <div>
                <p className="text-[9px] font-bold uppercase tracking-[0.18em] hw-royal-text">HoldWise Card Academy</p>
                <h1 className="font-heading text-lg font-black leading-tight text-[hsl(var(--hw-ink))]">{game?.title || 'Card Game'}</h1>
              </div>
            </div>
            <div className="flex items-center gap-1.5">
              <SensoryControls className="hidden sm:flex" />
              {actions}
              <TactilePressable onClick={() => navigate(`/game/${game?.id}/tutorial`)} className="hw-shell-icon rounded-xl p-2 shadow-none" aria-label={`Tutorial for ${game?.title || 'game'}`}><BookOpen size={18} /></TactilePressable>
              <TactilePressable onClick={() => navigate('/academy')} className="hw-shell-icon rounded-xl p-2 shadow-none" aria-label="Academy home"><Home size={18} /></TactilePressable>
            </div>
          </GlassSurface>
          <main className="mx-auto max-w-5xl">{children}</main>
          <div className="mx-auto mt-4 flex max-w-5xl items-center justify-between gap-3">
            <span className="flex items-center gap-1.5 text-[11px] text-[hsl(var(--hw-ink)/.58)]"><Sparkles size={13} className="hw-sunshine-text" /> Full play · Coach-enabled</span>
            <AskCoachButton context={coachContext} label="Ask Coach Ace" />
          </div>
        </div>
      );
    }
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///home/runner/work/holdwise-ai/holdwise-ai/tests/casino-premium-v7.test.mjs:52:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
1..5
# tests 5
# suites 0
# pass 0
# fail 5
# cancelled 0
# skipped 0
# todo 0
# duration_ms 139.13848
