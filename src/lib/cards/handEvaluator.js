export const HAND_CATEGORIES={ROYAL_FLUSH:"Royal Flush",STRAIGHT_FLUSH:"Straight Flush",FOUR_OF_A_KIND:"Four of a Kind",FULL_HOUSE:"Full House",FLUSH:"Flush",STRAIGHT:"Straight",THREE_OF_A_KIND:"Three of a Kind",TWO_PAIR:"Two Pair",HIGH_PAIR:"Jacks or Better",LOW_PAIR:"Low Pair",HIGH_CARD:"High Card"};
export const CATEGORY_RANK=["HIGH_CARD","LOW_PAIR","HIGH_PAIR","TWO_PAIR","THREE_OF_A_KIND","STRAIGHT","FLUSH","FULL_HOUSE","FOUR_OF_A_KIND","STRAIGHT_FLUSH","ROYAL_FLUSH"];
function rankCounts(cards){const m={};for(const c of cards)m[c.value]=(m[c.value]||0)+1;return m;}
function isFlush(cards){const s=cards[0].suit;return cards.every(c=>c.suit===s);}
function straightInfo(cards){const vals=cards.map(c=>c.value).sort((a,b)=>a-b);const unique=Array.from(new Set(vals));if(unique.length!==5)return{isStraight:false,high:0};if(vals[4]-vals[0]===4)return{isStraight:true,high:vals[4],aceLow:false};if(vals[0]===2&&vals[1]===3&&vals[2]===4&&vals[3]===5&&vals[4]===14)return{isStraight:true,high:5,aceLow:true};return{isStraight:false,high:0};}
export function evaluateHand(cards){if(!cards||cards.length!==5)throw new Error("Hand evaluator requires exactly 5 cards");const flush=isFlush(cards);const straight=straightInfo(cards);const counts=rankCounts(cards);const groups=Object.entries(counts).map(([v,n])=>({value:Number(v),count:n})).sort((a,b)=>b.count-a.value-a.value||0).sort((a,b)=>b.count-a.count||b.value-a.value);const has=n=>groups.some(g=>g.count===n);if(flush&&straight.isStraight&&straight.high===14)return{category:"ROYAL_FLUSH",name:HAND_CATEGORIES.ROYAL_FLUSH,rankIndex:CATEGORY_RANK.indexOf("ROYAL_FLUSH"),groups,flush,straight:true,highCard:14,aceLow:false};if(flush&&straight.isStraight)return{category:"STRAIGHT_FLUSH",name:HAND_CATEGORIES.STRAIGHT_FLUSH,rankIndex:CATEGORY_RANK.indexOf("STRAIGHT_FLUSH"),groups,flush,straight:true,highCard:straight.high,aceLow:straight.aceLow};if(has(4))return{category:"FOUR_OF_A_KIND",name:HAND_CATEGORIES.FOUR_OF_A_KIND,rankIndex:CATEGORY_RANK.indexOf("FOUR_OF_A_KIND"),groups,flush,straight:false,highCard:groups[0].value};if(has(3)&&has(2))return{category:"FULL_HOUSE",name:HAND_CATEGORIES.FULL_HOUSE,rankIndex:CATEGORY_RANK.indexOf("FULL_HOUSE"),groups,flush,straight:false,highCard:groups[0].value};if(flush){const high=Math.max(...cards.map(c=>c.value));return{category:"FLUSH",name:HAND_CATEGORIES.FLUSH,rankIndex:CATEGORY_RANK.indexOf("FLUSH"),groups,flush,straight:false,highCard:high};}if(straight.isStraight)return{category:"STRAIGHT",name:HAND_CATEGORIES.STRAIGHT,rankIndex:CATEGORY_RANK.indexOf("STRAIGHT"),groups,flush,straight:true,highCard:straight.high,aceLow:straight.aceLow};if(has(3))return{category:"THREE_OF_A_KIND",name:HAND_CATEGORIES.THREE_OF_A_KIND,rankIndex:CATEGORY_RANK.indexOf("THREE_OF_A_KIND"),groups,flush,straight:false,highCard:groups[0].value};const pairs=groups.filter(g=>g.count===2);if(pairs.length===2)return{category:"TWO_PAIR",name:HAND_CATEGORIES.TWO_PAIR,rankIndex:CATEGORY_RANK.indexOf("TWO_PAIR"),groups,flush,straight:false,highCard:Math.max(pairs[0].value,pairs[1].value)};if(pairs.length===1){const pv=pairs[0].value;if(pv>=11)return{category:"HIGH_PAIR",name:HAND_CATEGORIES.HIGH_PAIR,rankIndex:CATEGORY_RANK.indexOf("HIGH_PAIR"),groups,flush,straight:false,highCard:pv,isJacksOrBetter:true};return{category:"LOW_PAIR",name:HAND_CATEGORIES.LOW_PAIR,rankIndex:CATEGORY_RANK.indexOf("LOW_PAIR"),groups,flush,straight:false,highCard:pv};}const high=Math.max(...cards.map(c=>c.value));return{category:"HIGH_CARD",name:HAND_CATEGORIES.HIGH_CARD,rankIndex:CATEGORY_RANK.indexOf("HIGH_CARD"),groups,flush,straight:false,highCard:high};}
export function describeHand(result){return result.name;}
